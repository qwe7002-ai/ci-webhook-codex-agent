# Local secrets and config
.env
config.yaml
# Auto-generated webhook secret (persisted under codex.workdir).
.webhook-secret
**/.webhook-secret
# ...but the packaged default config is tracked (no secrets; uses ${VAR}).
!packaging/config.yaml

# Build output
/bin/
/ci-webhook-codex-agent
/dist/
/out/
/public/
*.deb

# Go
*.test
*.out

# Python bytecode
__pycache__/
*.pyc
